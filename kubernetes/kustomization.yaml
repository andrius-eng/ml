apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Root kustomization — aggregates all groups for local inspection.
# ArgoCD uses the per-group sub-apps instead of this file.
resources:
  - infra
  - data
  - ml
  - networking
  - monitoring

