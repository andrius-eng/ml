# GatewayClass is cluster-scoped — kept outside the Kustomization
# to prevent namespace injection by kustomize.
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: traefik
spec:
  controllerName: traefik.io/gateway-controller
