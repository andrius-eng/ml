{
  "name": "gunio-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "format": "prettier --write \"src/**/*.{js,css,html}\"",
    "check": "npm run build",
    "deploy": "gh-pages --dist dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^3.0.0",
    "vite": "^8.0.0"
  },
  "dependencies": {
    "chart.js": "^4.5.1"
  }
}
